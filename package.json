{
  "name": "houndrace-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typegenall": "typechain artifacts/contracts/hounds/Data.sol/HoundsData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/arenas/Data.sol/ArenasData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/genetics/Data.sol/GeneticsData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/incubator/Data.sol/IncubatorData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/ogars/Data.sol/HoundracePotions.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/races/races/Data.sol/RacesData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/races/generator/Data.sol/RaceGeneratorData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/randomness/vanilla/Data.sol/RandomnessVanillaData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads && typechain artifacts/contracts/incubator/Data.sol/IncubatorData.json --target=web3-v1 --out-dir=$TYPEGEN_ALL_PATH --always-generate-overloads"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stater-co/houndrace-contracts.git"
  },
  "keywords": [
    "houndrace",
    "houndrace",
    "contracts",
    "contracts",
    "crypto"
  ],
  "author": "Gheonea Emilian",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stater-co/houndrace-contracts/issues"
  },
  "homepage": "https://github.com/stater-co/houndrace-contracts#readme",
  "dependencies": {
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "@openzeppelin/contracts": "^4.4.2",
    "@typechain/ethers-v5": "^9.0.0",
    "@typechain/web3-v1": "^5.0.0",
    "ethereumjs-tx": "^2.1.2",
    "hardhat": "^2.8.3",
    "hardhat-contract-sizer": "^2.4.0",
    "hardhat-gas-reporter": "^1.0.7",
    "hardhat-tracer": "^1.0.0-alpha.6",
    "solidity-coverage": "^0.7.18",
    "typechain": "^7.0.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-waffle": "^2.0.2",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.4"
  }
}
